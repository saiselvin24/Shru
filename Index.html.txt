<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>For My Papa ❤️</title>
    <style>
        :root {
            --deep-red: #800020;
            --rose: #ff4d6d;
            --soft-pink: #fff0f3;
            --gold: #ffb703;
            --glass: rgba(255, 255, 255, 0.2);
        }

        body, html {
            margin: 0; padding: 0;
            background: linear-gradient(135deg, #ff4d6d 0%, #800020 100%);
            font-family: 'Segoe UI', Roboto, Helvetica, Arial, sans-serif;
            color: white;
            overflow-x: hidden;
            text-align: center;
        }

        /* Screens */
        .screen { 
            display: none; flex-direction: column; align-items: center; justify-content: center;
            min-height: 100vh; width: 100vw; padding: 25px; box-sizing: border-box;
            animation: fadeIn 0.8s ease; 
        }
        .active { display: flex; }

        @keyframes fadeIn { from { opacity: 0; transform: scale(0.95); } to { opacity: 1; transform: scale(1); } }

        /* Navigation Buttons */
        .btn-wrap { position: relative; height: 100px; width: 100%; margin-top: 25px; }
        button { 
            padding: 16px 45px; font-size: 1.1rem; border-radius: 50px; border: none; 
            cursor: pointer; transition: 0.3s; font-weight: 700; text-transform: uppercase;
            letter-spacing: 1px;
        }
        .primary-btn { background: white; color: var(--deep-red); box-shadow: 0 10px 20px rgba(0,0,0,0.2); }
        .back-btn { 
            background: var(--glass); color: white; border: 1px solid white;
            margin-top: 35px; padding: 10px 30px; font-size: 0.8rem;
        }
        #noBtn { background: #333; color: white; position: absolute; }

        /* Visual Elements */
        .bear-img { width: 260px; border-radius: 30px; margin: 25px 0; border: 5px solid white; box-shadow: 0 15px 30px rgba(0,0,0,0.3); }
        .gift-menu { display: grid; grid-template-columns: 1fr 1fr; gap: 20px; margin-top: 30px; width: 100%; max-width: 400px; }
        
        .gift-item {
            background: var(--glass); border: 2px solid white; border-radius: 25px;
            padding: 25px 15px; cursor: pointer; transition: 0.3s;
        }
        .gift-item:hover { background: white; color: var(--deep-red); transform: translateY(-5px); }
        .gift-item span { font-size: 35px; display: block; margin-bottom: 10px; }

        .content-card {
            background: white; color: #333; padding: 40px; border-radius: 40px;
            max-width: 650px; width: 95%; box-shadow: 0 25px 50px rgba(0,0,0,0.2);
            position: relative;
        }

        /* Timer & Details */
        #timer { font-size: 1.4rem; color: var(--rose); margin: 20px 0; font-weight: 800; border-bottom: 2px solid var(--soft-pink); padding-bottom: 15px; }
        .letter-body { text-align: left; line-height: 1.7; white-space: pre-wrap; font-size: 1.05rem; max-height: 50vh; overflow-y: auto; padding-right: 10px; }
        .reasons { text-align: left; list-style: none; padding: 0; }
        .reasons li { margin-bottom: 12px; font-weight: 500; color: #555; }
        .reasons li::before { content: "❤️ "; }

        .heart { position: fixed; top: -10px; color: white; font-size: 20px; pointer-events: none; animation: fall linear forwards; z-index: 100; }
        @keyframes fall { to { transform: translateY(105vh) rotate(360deg); } }
    </style>
</head>
<body>

    <div id="screen1" class="screen active">
        <h1 style="font-size: 3.2rem; margin-bottom: 10px;">Happy Valentine's Day, Shruthi! ❤️</h1>
        <p style="font-size: 1.3rem; font-weight: 300;">My world is more beautiful with you, Papa.</p>
        <div class="btn-wrap">
            <button class="primary-btn" onclick="showScreen('screen2')">Will you be my Valentine?</button>
            <button id="noBtn" onmouseover="dodge()" ontouchstart="dodge()">No</button>
        </div>
    </div>

    <div id="screen2" class="screen">
        <h1 style="font-size: 2.8rem;">I Knew You'd Say Yes! 🥰</h1>
        <img src="bears.gif" alt="Hugging Bears" class="bear-img">
        <p style="font-size: 1.2rem; margin-bottom: 30px;">I love you more than words can say, Papa.</p>
        <button class="primary-btn" onclick="showScreen('screen3')">Open My Heart 🎁</button>
    </div>

    <div id="screen3" class="screen">
        <h1 style="margin-bottom: 5px;">Gifts for Papa ❤️</h1>
        <p>Pick a gift to see how much I care</p>
        <div class="gift-menu">
            <div class="gift-item" onclick="showScreen('gift-music')"><span>🎵</span>Voice</div>
            <div class="gift-item" onclick="showScreen('gift-letter')"><span>✉️</span>Letter</div>
            <div class="gift-item" onclick="showScreen('gift-reasons')" style="grid-column: span 2;"><span>✨</span>Our Forever</div>
        </div>
        <button class="back-btn" onclick="showScreen('screen2')">← Back</button>
    </div>

    <div id="gift-music" class="screen">
        <div class="content-card">
            <h2 style="color: var(--rose);">Your Angelic Voice 🎵</h2>
            <audio id="player" controls style="width: 100%; margin: 25px 0;">
                <source src="Shruthi’s singing .mp3" type="audio/mpeg">
            </audio>
            <p style="color: #666;">This melody is my favorite place to be. 💖</p>
            <button class="back-btn" style="background: var(--rose); border: none;" onclick="showScreen('screen3')">Return to Gifts</button>
        </div>
    </div>

    <div id="gift-letter" class="screen">
        <div class="content-card">
            <h2 style="color: var(--rose); margin-top: 0;">My Deepest Truth ✉️</h2>
            <div class="letter-body">I love you papa and Uk it very well and even u have said it urself no one can love you like I do ik I have made mistake but I’m sorry for everything.

So I really want us not for anything else but only to love you truly and have the life we ever dreamed off so let’s fix us and I really love you papa I’m sorry I mean it ur always special to me and will always be I have never thought of losing u even now I can’t let us go apart coz that is not what we are and ik and uk that as well as I do.

I want to say one thing I love you for real not for my benefit I promise that not even a single thought like that ur my dream girl and I have said this to u also Uk it well ur my dream girl I love you more than anything and anyone I had just see how happy we where if both had love only we can be happy not only one person right our time and winds are turned into mess but if we work it out together we can fix this together and I promise I will never repeat that mistake ever in my life 😩🩵💯</div>
            <button class="back-btn" style="background: var(--rose); border: none;" onclick="showScreen('screen3')">Return to Gifts</button>
        </div>
    </div>

    <div id="gift-reasons" class="screen">
        <div class="content-card">
            <h2 style="color: var(--rose); margin-top: 0;">Our Beautiful Journey ✨</h2>
            <div id="timer">Counting our moments...</div>
            <ul class="reasons">
                <li>You are, and will always be, my absolute dream girl.</li>
                <li>No one else could ever love you with the depth that I do.</li>
                <li>We deserve the happy life we've always dreamed of together.</li>
                <li>I am committed to fixing us because losing you isn't an option.</li>
                <li>I promise on my life to never repeat the mistakes of the past.</li>
            </ul>
            <button class="back-btn" style="background: var(--rose); border: none;" onclick="showScreen('screen3')">Return to Gifts</button>
        </div>
    </div>

    <script>
        // Start date: April 3, 2023, at 1:30 AM
        const startDate = new Date(2023, 3, 3, 1, 30, 0); 

        function dodge() {
            const btn = document.getElementById('noBtn');
            btn.style.left = Math.random() * (window.innerWidth - btn.offsetWidth) + 'px';
            btn.style.top = Math.random() * (window.innerHeight - btn.offsetHeight) + 'px';
        }

        function showScreen(id) {
            document.querySelectorAll('.screen').forEach(s => s.classList.remove('active'));
            document.getElementById(id).classList.add('active');
            
            if(id === 'screen2') setInterval(rainHearts, 400);
            if(id === 'gift-music') document.getElementById('player').play();
            if(id === 'gift-reasons') startTimer();
        }

        function startTimer() {
            setInterval(() => {
                const now = new Date();
                const diff = now - startDate;
                const d = Math.floor(diff / (1000 * 60 * 60 * 24));
                const h = Math.floor((diff / (1000 * 60 * 60)) % 24);
                const m = Math.floor((diff / (1000 * 60)) % 60);
                const s = Math.floor((diff / 1000) % 60);
                document.getElementById('timer').innerHTML = 
                    `${d} Days, ${h} Hours, ${m} Minutes, ${s} Seconds`;
            }, 1000);
        }

        function rainHearts() {
            const h = document.createElement('div');
            h.classList.add('heart'); h.innerHTML = '❤️';
            h.style.left = Math.random() * 100 + 'vw';
            h.style.animationDuration = Math.random() * 3 + 2 + 's';
            document.body.appendChild(h);
            setTimeout(() => h.remove(), 5000);
        }
    </script>
</body>
</html>
